# Vercel / local frontend — copy to .env.local (Next.js) or .env (Vite)
# Public URL of the Railway FastAPI service (no trailing slash).

NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Optional: display name in UI header
NEXT_PUBLIC_APP_NAME=ICICI Prudential Mutual Funds — Facts Assistant
