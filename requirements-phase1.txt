# Phase 1+ — corpus ingest, local embeddings & vector store (install after Phase 0)
-r requirements.txt
chromadb>=0.5.0,<1.0
sentence-transformers>=3.0.0,<4.0

# Note: chromadb may require Microsoft C++ Build Tools on Windows.
# See https://visualstudio.microsoft.com/visual-cpp-build-tools/
# First ingest run downloads BAAI/bge-small-en-v1.5 from Hugging Face (~130 MB).
